#include <stdio.h>
/**
 * main - prints all possible combinations of 2 digits.
 * Numbers
 * Return: 0
 */
int main(void)
{
    int i, p;
    
    
    for (1 = 0; 1 < 100; i++)
    {
           
        for (p = 0; p < 100; i++)
                        { 

               If (p > I)
                   {
                   putchar((i / 10) + '0');
                   putchar((i % 10) + '0');
                   putchar(' ');
                                          putchar((p / 10) + '0')
                                           Putchar((p % 10) + '0' );
                          If (i  l= 98)
            {
                putchar(',');
                putchar(' ');
            }
          }
    
                }
    
       }
putchar('\n');
return (0);
}


